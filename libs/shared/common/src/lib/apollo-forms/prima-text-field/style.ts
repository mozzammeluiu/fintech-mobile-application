import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontWeight: 'bold',
    color: 'black',
    fontFamily: 'Open Sans',
    fontSize: 20,
  },
});

export { styles };
